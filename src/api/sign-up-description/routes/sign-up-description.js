'use strict';

/**
 * sign-up-description router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::sign-up-description.sign-up-description');
