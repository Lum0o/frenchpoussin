'use strict';

/**
 * sign-up-description service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sign-up-description.sign-up-description');
